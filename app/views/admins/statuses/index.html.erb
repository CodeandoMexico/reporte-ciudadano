<h1>Estatus actuales</h1>

<table border="0">
   <tr>
     <th>Nombre</th>
     <th>Editar</th>
   </tr>

   <%- @statuses.each do |status| %>
    <tr>
      <td><%= status.name %></td>
      <td><%= link_to edit_admins_status_path(status) %></td>
    </tr>
   <% end %>
</table>
