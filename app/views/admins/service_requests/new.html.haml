.content-header
  %h2= t('.header')

.page-content.inset
  .row
    .col-md-12
      = form_for [:admins, @service_request], html: { class: "form-horizontal", role: 'form'} do |f|
        = render 'form', f: f
